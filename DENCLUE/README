This programming is written in the environemnt of python 2.7 under Ubantu 14.0.4
If you want to run the program on you computer, you should first install numpy and matplotlib(two libraries for python) in your computer
Each time when you want to run the program, you should type the command in the terminal like:
python DENCLUE.py <inputfile>
where <inputfile> should be replaced as the name of your input file name
And the program will show the number of the clusters in the terminal, and plot a spotted figure.